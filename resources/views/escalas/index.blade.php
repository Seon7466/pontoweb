<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Escalas</h2></x-slot><x-ui.alert />
<div class="py-8"><div class="max-w-7xl mx-auto sm:px-6 lg:px-8"><div class="mb-4 flex flex-col gap-3 sm:flex-row sm:justify-between">
<form method="GET" class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Buscar escala" class="rounded-md border-gray-300 shadow-sm"><button class="px-4 py-2 bg-gray-700 text-white rounded">Buscar</button></form>
<a href="{{ route('escalas.create') }}" class="px-4 py-2 bg-blue-600 text-white rounded text-center">Nova Escala</a></div>
<div class="bg-white shadow rounded p-6 overflow-x-auto"><table class="w-full"><thead><tr><th class="border-b p-2 text-left">Descrição</th><th class="border-b p-2 text-left">Tipo</th><th class="border-b p-2 text-left">Dias</th><th class="border-b p-2 text-left">Status</th><th class="border-b p-2 text-left">Ações</th></tr></thead><tbody>
@forelse($items as $item) @php($dias = collect(['Dom'=>$item->domingo,'Seg'=>$item->segunda,'Ter'=>$item->terca,'Qua'=>$item->quarta,'Qui'=>$item->quinta,'Sex'=>$item->sexta,'Sáb'=>$item->sabado])->filter()->keys()->join(', '))
<tr><td class="border-b p-2">{{ $item->descricao }}</td><td class="border-b p-2">{{ $item->tipo }}</td><td class="border-b p-2">{{ $dias ?: '-' }}</td><td class="border-b p-2">{{ $item->ativo ? 'Ativa' : 'Inativa' }}</td><td class="border-b p-2"><div class="flex gap-3"><a class="text-blue-600" href="{{ route('escalas.edit',$item) }}">Editar</a><form method="POST" action="{{ route('escalas.destroy',$item) }}" onsubmit="return confirm('Excluir esta escala?')">@csrf @method('DELETE')<button class="text-red-600">Excluir</button></form></div></td></tr>
@empty<tr><td colspan="5" class="p-4 text-center text-gray-500">Nenhuma escala encontrada.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $items->links() }}</div></div></div></div></x-app-layout>
