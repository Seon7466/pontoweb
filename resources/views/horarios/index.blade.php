<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Horários</h2></x-slot><x-ui.alert />
<div class="py-8"><div class="max-w-7xl mx-auto sm:px-6 lg:px-8"><div class="mb-4 flex flex-col gap-3 sm:flex-row sm:justify-between">
<form method="GET" class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Buscar horário" class="rounded-md border-gray-300 shadow-sm"><button class="px-4 py-2 bg-gray-700 text-white rounded">Buscar</button></form>
<a href="{{ route('horarios.create') }}" class="px-4 py-2 bg-blue-600 text-white rounded text-center">Novo Horário</a></div>
<div class="bg-white shadow rounded p-6 overflow-x-auto"><table class="w-full"><thead><tr><th class="border-b p-2 text-left">Descrição</th><th class="border-b p-2 text-left">Entrada</th><th class="border-b p-2 text-left">Intervalo</th><th class="border-b p-2 text-left">Saída</th><th class="border-b p-2 text-left">Ações</th></tr></thead><tbody>
@forelse($items as $item)<tr><td class="border-b p-2">{{ $item->descricao }}</td><td class="border-b p-2">{{ substr($item->entrada,0,5) }}</td><td class="border-b p-2">{{ $item->inicio_intervalo ? substr($item->inicio_intervalo,0,5).' - '.substr($item->fim_intervalo,0,5) : '-' }}</td><td class="border-b p-2">{{ substr($item->saida,0,5) }}</td><td class="border-b p-2"><div class="flex gap-3"><a class="text-blue-600" href="{{ route('horarios.edit',$item) }}">Editar</a><form method="POST" action="{{ route('horarios.destroy',$item) }}" onsubmit="return confirm('Excluir este horário?')">@csrf @method('DELETE')<button class="text-red-600">Excluir</button></form></div></td></tr>
@empty<tr><td colspan="5" class="p-4 text-center text-gray-500">Nenhum horário encontrado.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $items->links() }}</div></div></div></div></x-app-layout>
